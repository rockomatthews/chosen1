## Server (Next.js API routes)
# CryptoCompare API key (recommended). Without it, free rate limits may break 5s polling.
CRYPTOCOMPARE_API_KEY=

## Client (used by Ramp iframe)
# Ramp "host API key" for widget embed.
NEXT_PUBLIC_RAMP_WIDGET_API_KEY=


